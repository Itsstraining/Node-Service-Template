function sampleMiddleware(req, res, next) {
  // Step 1. Get request
  // Step 2. Processing
  // Step 3.1 Respond if rejected
  // Step 3.2 Call next() if continue
}

module.exports = sampleMiddleware;
